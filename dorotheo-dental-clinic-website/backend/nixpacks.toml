[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["pip3 install --upgrade pip", "pip3 install -r requirements.txt"]

[phases.build]
cmds = ["python3 manage.py collectstatic --noinput"]

[start]
cmd = "python3 manage.py migrate && gunicorn dental_clinic.wsgi"
