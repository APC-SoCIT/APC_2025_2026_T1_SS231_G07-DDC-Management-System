# Django Settings
# IMPORTANT: Keep this SECRET_KEY secure! Never commit it to Git!
SECRET_KEY=your-secret-key-here
DEBUG=True

# Database Configuration
# For LOCAL DEVELOPMENT: Comment out DATABASE_URL to use SQLite (faster for testing)
# For PRODUCTION (Railway/Vercel): Uncomment the line below
# DATABASE_URL=postgresql://user:password@host:port/database

# Allowed Hosts (comma-separated, no spaces)
# Add your Railway domain and Vercel domain here
ALLOWED_HOSTS=*.railway.app,*.vercel.app,localhost,127.0.0.1

# CORS Settings - Allow all origins for now (you can restrict later)
CORS_ALLOW_ALL_ORIGINS=True

# CSRF Trusted Origins (comma-separated, no spaces)
# Add your Railway backend URL and Vercel frontend URL here
CSRF_TRUSTED_ORIGINS=https://your-backend.railway.app,https://your-frontend.vercel.app,http://localhost:3000,http://localhost:8000

# ============================================
# EMAIL CONFIGURATION
# ============================================

# Choose your email backend:
# For development (console - prints to terminal, no real emails sent):
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# For sending real emails via Gmail SMTP:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-gmail-app-password-here

# For production, consider using:
# - SendGrid: EMAIL_HOST=smtp.sendgrid.net, EMAIL_HOST_USER=apikey
# - AWS SES: EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# - Mailgun: EMAIL_HOST=smtp.mailgun.org
# - Postmark: EMAIL_HOST=smtp.postmarkapp.com

# Sender information
# This appears in the "From" field of emails
DEFAULT_FROM_EMAIL=Dorotheo Dental Clinic <your-email@gmail.com>

# Admin notification emails - receives BCC copies of ALL system emails
# Set this to your admin/monitoring email address(es)
# Multiple emails: separate with commas (no spaces)
# Example: ADMIN_NOTIFICATION_EMAIL=admin@clinic.com,manager@clinic.com
ADMIN_NOTIFICATION_EMAIL=your-admin-email@example.com

# ============================================
# RESEND EMAIL API (Recommended for Production)
# ============================================
# Railway blocks SMTP ports on free tier, so we use Resend's HTTP API instead
# Sign up at: https://resend.com (Free tier: 100 emails/day)
# Get your API key from: https://resend.com/api-keys

# For production with Resend (recommended for Railway):
# Uncomment these lines and add your Resend API key:
# EMAIL_BACKEND=api.resend_backend.ResendEmailBackend
# RESEND_API_KEY=re_your_resend_api_key_here
# DEFAULT_FROM_EMAIL=Dorotheo Dental Clinic <noreply@yourdomain.com>

# Note: You need to verify your sending domain in Resend dashboard
# or use their default onboarding@resend.dev for testing

# ============================================
# FRONTEND URL (REQUIRED for Password Reset)
# ============================================
# This is your Vercel deployment URL where your Next.js frontend is hosted
# Used for generating password reset links that redirect users back to your site
# IMPORTANT: Must be set in production for password reset to work!
FRONTEND_URL=https://your-frontend.vercel.app

# For local development:
# FRONTEND_URL=http://localhost:3000

# ============================================
# GMAIL SMTP SETUP INSTRUCTIONS
# ============================================
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Go to: Google Account → Security → 2-Step Verification
# 3. Scroll down to "App passwords" and click
# 4. Select "Mail" and "Other (Custom name)"
# 5. Enter name: "Django Dental Clinic"
# 6. Copy the 16-character password (format: xxxx xxxx xxxx xxxx)
# 7. Remove spaces and use as EMAIL_HOST_PASSWORD
# 8. IMPORTANT: This is NOT your regular Gmail password!

# ============================================
# EMAIL NOTIFICATION FEATURES
# ============================================
# The system sends emails for:
# - Appointment confirmations (instant)
# - Appointment reminders (24h before via cron job)
# - Appointment cancellations
# - Reschedule approvals/rejections
# - Invoices
# - Payment confirmations
# - Payment reminders (overdue)
# - Low stock alerts (inventory)
# - Staff notifications (new appointments)
#
# All emails are sent in background threads for instant UI response.
# Admin emails receive BCC copies of ALL system emails.

# ============================================
# AUTOMATED REMINDERS SETUP
# ============================================
# To enable automated reminders, set up cron jobs:
#
# Appointment Reminders (daily at 9 AM):
# 0 9 * * * cd /path/to/backend && python manage.py send_appointment_reminders
#
# Payment Reminders (weekly on Monday at 10 AM):
# 0 10 * * 1 cd /path/to/backend && python manage.py send_payment_reminders
#
# Or use Railway cron jobs in railway.yml

# ============================================
# TESTING EMAILS DURING DEVELOPMENT
# ============================================
# 1. Console Backend (no real emails, prints to terminal):
#    EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
#    python test_emails.py
#
# 2. Real Emails (uses Gmail/SMTP):
#    EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
#    Set EMAIL_HOST_USER and EMAIL_HOST_PASSWORD
#    Book an appointment via the portal
#
# 3. Test email addresses:
#    - testmail.app: Create inbox at https://testmail.app/
#    - mailtrap.io: Development email testing service
#    - Gmail: Use your own Gmail for testing

# ============================================
# AI CHATBOT CONFIGURATION
# ============================================

# Google Gemini API Key
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here
