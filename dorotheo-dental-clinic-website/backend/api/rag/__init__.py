"""
RAG (Retrieval Augmented Generation) module for the AI chatbot.

Provides page-index-based context retrieval to ground AI responses
with real site content. Acts as an optional enhancement layer —
if retrieval fails, the chatbot falls back to its existing behavior.
"""
